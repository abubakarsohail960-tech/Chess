# Local development (SQLite — works out of the box)
DATABASE_URL="file:./prisma/dev.db"

# Vercel production: use Neon PostgreSQL (see README)
# DATABASE_URL="postgresql://user:password@host.neon.tech/chess?sslmode=require"

# Generate with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Public app URL (set in Vercel env vars)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
